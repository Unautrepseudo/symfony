{% extends 'base.html.twig' %}

{% block title %}Friend{% endblock %}

{% block body %}
    <h1>Friend</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Dimitru</th>
                <td>{{ friend.dimitru }}</td>
            </tr>
            <tr>
                <th>Miyako</th>
                <td>{{ friend.miyako }}</td>
            </tr>
            <tr>
                <th>Castormalin</th>
                <td>{{ friend.castormalin }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ friend.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('friend_index') }}">back to list</a>

    <a href="{{ path('friend_edit', {'id': friend.id}) }}">edit</a>

    {{ include('friend/_delete_form.html.twig') }}
{% endblock %}
